Харківський національний університет радіоелектроніки
Факультет комп’ютерних наук
Катедра програмної інженерії
 
 
Звіт
з практичної роботи № 1 
з дисципліни «Скриптові мови програмування»

 
 
 
Виконав:                                                                           Перевірив:
ст. гр. ПЗПІ-23-4                                                              ст. викладач кафедри ПІ
Цисельський Андрій                                            	         Сокорчук Ігор Петрович
 
 
 
Харків 2025
1 ІСТОРІЯ ЗМІН

№ДатаВерсія звітуОпис змін та виправлень105.04.20250.1Зроблено звіт
2 ЗАВДАННЯ
     Написати Bash скрипт, який друкуватиме на екрані ялинку із двох ярусів гілок, стовбура та шару снігу.
     Ялинка повинна бути симетричною.
     Яруси гілок повинні утворювати правильні рівнобедрені трикутники у яких сусідні рядки відрізняються на два символи та складаються почергово або з символів «*» або з символів «#». 
     Ширина усіх ярусів гілок повинна бути на 2 символи вужча ніж ширина снігу.
     Висота стовбура та товщина снігу мають бути завжди такі, як у прикладі.
     Висота ялинки у рядках разом з шаром снігу та ширина шару снігу в символах вказується скрипту в його параметрах при запуску.
     Параметри скрипта повинні мати додатнє значення.
     Вказані значення повинні округлятися до потрібних у меншу сторону.
     Якщо за вказаними при запуску скрипта значеннями зобразити ялинку на екрані неможливо, скрипт повинен вивести у потік помилок сповіщення про неправильне значення аргумента і повернути відповідний результат у батьківський процес.
     У скрипті потрібно обовʼязково використати такі конструкції: 
     if ... then ... fi
     while ... do ... done
     until ... do ... done
     for ... in .. do ... done
     for ((...)); do ... done
     У скрипті також потрібно використати функцію

3 ОПИС ВИКОНАНОЇ РОБОТИ
     З допомогою команди nano було створено та відкрито для редагування новий пустий файл з відповідною назвою smp-pzpi-23-4-tsyselskyi-andrii-pract1-code.
     Весь код для побудови ялинки за параметрами висоти та ширини снігу знаходиться у додатку В. Посилання на відеозапис функціонування програмного застосунку наведено у додатку А. Початковий рядок скрипта — це shebang, який повідомляє системі, яку програму слід використати для запуску цього скрипта.
     У рядках 1-18 оголошується фнкція draw_line, яка відповідає за виведення одного рядка з певною кількістю пробілів та символів.
1 #!/bin/bash
2 
3 draw_line() {
4     local spaces=$1
5     local count=$2
6     local char=$3
7 
8     local s=0
9     while [ $s -lt $spaces ]; do
10         echo -n " "
11         s=$((s + 1))
12     done
13 
14     for sym in $(seq 1 $count); do
15         echo -n "$char"
16     done
17     echo
18 }

     У рядках 20-61 оголошується фeнкція validate_input, яка виконує повну перевірку правильності вхідних параметрів для побудови ялинки.
     
20 validate_input() {
21     if [ "$#" -ne 2 ]; then
22         echo "Необхідно вказати висоту дерева та ширину снігу!" >&2
23         exit 1
24     fi
25 
26 	if ! [[ "$1" =~ ^[0-9]+$ && "$2" =~ ^[0-9]+$ ]]; then
27         echo "Висота та сніг мають бути числами!" >&2
28         exit 2
29     fi
30 
31     if [ "$1" -le 0 ] || [ "$2" -le 0 ]; then
32         echo "Параметри мають бути більше нуля!" >&2
33         exit 3
34     fi
35 
36     if [ "$2" -lt 7 ] || [ "$1" -lt 8 ]; then
37         echo "Неможливо побудувати ялинку!" >&2
38         exit 4
39     fi
40 
41     local delta=$(( $1 - $2 ))
42 
43     case "$delta" in
44         0|1|2) ;;
45         *) echo "Неможливо побудувати ялинку!" >&2; exit 5 ;;
46     esac
47 
48     if [ $(( $2 % 2 )) -ne 0 ] && [ $(( $1 % 2 )) -ne 0 ]; then
49         if [ "$delta" -eq 0 ]; then
50             echo "Неможливо побудувати ялинку!" >&2
51             exit 6
52         fi
53     fi
54 
55     if [ $(( $2 % 2 )) -eq 0 ] && [ $(( $1 % 2 )) -eq 0 ]; then
56 		if [ "$delta" -eq 2 ]; then
57 			echo "Неможливо побудувати ялинку!" >&2
58 			exit 7
59 		fi
60 	fi
61 }

     У рядках 63-100 виконується побудова ялинки: верхня і нижня частини гілок створюються у два цикли з чергуванням символів і симетричним вирівнюванням. Далі додається стовбур, що складається з двох рядків символів #, та основа зі снігу у вигляді суцільного ряду зірочок.
     
63 H=$1
64 W=$2
65 
66 validate_input "$H" "$W"
67 
68 symbol="*"
69 symbol_width=$((W - 2))
70 triangle_part=$(((H - 2) / 2))
71 
72 if [ $((W % 2)) -eq 0 ]; then
73     W=$((W - 1))
74 fi
75 
76 for num in $(seq 1 2 "$symbol_width"); do
77     spc=$(( (W - num) / 2 ))
78     draw_line "$spc" "$num" "$symbol"
79     symbol=$([[ "$symbol" = "*" ]] && echo "#" || echo "*")
80 done
81 
82 symbol=$([[ "$symbol" = "*" ]] && echo "#" || echo "*")
83 
84 for ((n=3; n<=symbol_width; n+=2)); do
85     spc=$(( (W - n) / 2 ))
86     symbol=$([[ "$symbol" = "*" ]] && echo "#" || echo "*")
87     draw_line $spc $n "$symbol"
88 done
89 
90 for _ in 1 2; do
91     side=$(( (W - 3) / 2 ))
92     draw_line $side 3 "#"
93 done
94 
95 snow=0
96 until [ $snow -ge $W ]; do
97     echo -n "*"
98     snow=$((snow + 1))
99 done
100 echo


     У коді було використано усі види циклів та функції, визначені умовою.
     Після створення скрипта файл був збережений та за допомогою команд chmod a+x та chmod go-w зроблено виконуваним з можливістю редагування лише для власника.
     
4 ВИСНОВКИ
     Під час виконання цієї роботи я опанував написання Bash-скриптів із використанням умовних операторів, різних типів циклів, функцій та обробки вхідних аргументів. Також реалізував алгоритм побудови симетричної ялинки на основі заданих висоти та ширини. Здобув навички перевірки коректності введених даних, перенаправлення повідомлень про помилки та налаштування прав доступу до скрипту відповідно до технічних вимог.
ДОДАТОК А
Відеозапис
Відеозапис презентації результатів лабораторної роботи: https://youtu.be/1-iDnlJ6hxU
Хронологічний опис відеозапису:
00:00 - Вступ
00:11 - Завдання
00:23 - Розгляд програми
02:00 - Розгляд коду
















ДОДАТОК В
Програмний код
GitHub репозиторій: https://github.com/NureTsyselskyiAndrii/smp-pzpi-23-4-tsyselskyi-andrii/blob/main/Pract1/smp-pzpi-23-4-tsyselskyi-andrii-pract1/smp-pzpi-23-4-tsyselskyi-andrii-pract1-code
1. #!/bin/bash
2. 
3. draw_line() {
4.     local spaces=$1
5.     local count=$2
6.     local char=$3
7. 
8.     local s=0
9.     while [ $s -lt $spaces ]; do
10.         echo -n " "
11.         s=$((s + 1))
12.     done
13. 
14.     for sym in $(seq 1 $count); do
15.         echo -n "$char"
16.     done
17.     echo
18. }
19. 
20. validate_input() {
21.     if [ "$#" -ne 2 ]; then
22.         echo "Необхідно вказати висоту дерева та ширину снігу!" >&2
23.         exit 1
24.     fi
25. 
26. 	if ! [[ "$1" =~ ^[0-9]+$ && "$2" =~ ^[0-9]+$ ]]; then
27.         echo "Висота та сніг мають бути числами!" >&2
28.         exit 2
29.     fi
30. 
31.     if [ "$1" -le 0 ] || [ "$2" -le 0 ]; then
32.         echo "Параметри мають бути більше нуля!" >&2
33.         exit 3
34.     fi
35. 
36.     if [ "$2" -lt 7 ] || [ "$1" -lt 8 ]; then
37.         echo "Неможливо побудувати ялинку!" >&2
38.         exit 4
39.     fi
40. 
41.     local delta=$(( $1 - $2 ))
42. 
43.     case "$delta" in
44.         0|1|2) ;;
45.         *) echo "Неможливо побудувати ялинку!" >&2; exit 5 ;;
46.     esac
47. 
48.     if [ $(( $2 % 2 )) -ne 0 ] && [ $(( $1 % 2 )) -ne 0 ]; then
49.         if [ "$delta" -eq 0 ]; then
50.             echo "Неможливо побудувати ялинку!" >&2
51.             exit 6
52.         fi
53.     fi
54. 
55.     if [ $(( $2 % 2 )) -eq 0 ] && [ $(( $1 % 2 )) -eq 0 ]; then
56. 		if [ "$delta" -eq 2 ]; then
57. 			echo "Неможливо побудувати ялинку!" >&2
58. 			exit 7
59. 		fi
60. 	fi
61. }
62. 
63. H=$1
64. W=$2
65. 
66. validate_input "$H" "$W"
67. 
68. symbol="*"
69. symbol_width=$((W - 2))
70. triangle_part=$(((H - 2) / 2))
71. 
72. if [ $((W % 2)) -eq 0 ]; then
73.     W=$((W - 1))
74. fi
75. 
76. for num in $(seq 1 2 "$symbol_width"); do
77.     spc=$(( (W - num) / 2 ))
78.     draw_line "$spc" "$num" "$symbol"
79.     symbol=$([[ "$symbol" = "*" ]] && echo "#" || echo "*")
80. done
81. 
82. symbol=$([[ "$symbol" = "*" ]] && echo "#" || echo "*")
83. 
84. for ((n=3; n<=symbol_width; n+=2)); do
85.     spc=$(( (W - n) / 2 ))
86.     symbol=$([[ "$symbol" = "*" ]] && echo "#" || echo "*")
87.     draw_line $spc $n "$symbol"
88. done
89. 
90. for _ in 1 2; do
91.     side=$(( (W - 3) / 2 ))
92.     draw_line $side 3 "#"
93. done
94. 
95. snow=0
96. until [ $snow -ge $W ]; do
97.     echo -n "*"
98.     snow=$((snow + 1))
99. done
100. echo



2










